'''

2. Python is a dynamically typed programming language:

    -> A variable is a container that stores a value  [WRONG MENTAL MODEL].
    -> A variable is just a name that refers (points) to an Object.

    What's an Object? (you may ask):
        An object is a 'chunk of memory' that contains:
            - A value
            - A type
            - A reference count (for memory management, when reference count == 0...bye bye from memory [for optimization purposes]).


'''
